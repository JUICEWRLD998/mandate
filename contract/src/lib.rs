//! z-mandate — TEE contract for MANDATE (onboard-customer / pay-invoice).
//! world.wit + wit-bindgen Guest impl arrive in Phase 2; this crate must stay
//! native-compilable (cargo build / cargo test) without them.
